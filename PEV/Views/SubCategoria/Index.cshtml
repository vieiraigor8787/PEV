@{
    @model List<tb_subcategoria>;
    ViewData["Title"] = "Home Page";

    Layout = "_Layout_dashboard";

}

<div >
    <table class="table">
        <tr>
            <th><h4>Sub-Categorias</h4></th>
            <th></th>
            <th></th>
            <th class="text-sm-right"><button class="btn btn-primary" type="button" onclick="window.location.href ='/subcategoria/cadastrosubcategoria'">Nova Sub-Categoria</button>
            <th>
        </tr>
    </table>
    @if (Model.Count > 0)
    {
        <table class="table">

            <thead>
                <tr>
                    <th>Código</th>
                    <th>Nome</th>
                    <th>Aparece no Menu</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.CodigoSubCategoria</td>
                        <td>@item.Nome</td>
                        <td>@item.Menu</td>
                        <td><button type="button" class="btn btn-warning" onclick="window.location.href='/categoria/editar?CodigoCategoria=@item.CodigoSubCategoria&Descricao=@item.Nome'">Editar</button></td>
                        <td><button type="button" class="btn btn-danger" onclick="window.location.href='/categoria/desativar?CodigoCategoria=@item.CodigoSubCategoria'">Excluir</button></td>

                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div class="alert alert-primary" role="alert">
            Sem Registros!
        </div>
    }

</div>




